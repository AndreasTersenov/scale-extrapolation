# PLAN — Phase 3: CLOSE THE LAST TIER (the method-paper phase)

Opened 2026-07-28 on Andreas's explicit direction: "I don't want a paper on an
audit or a negative result. I want this to be an actual method that works."
This is the reopening sign-off the R36 close required. The GOAL of this phase
is a method paper: the generator calibrated on every statistic the paper
claims usable — including the peak function — with the audit as its validation
section, not its headline. The paper skeleton is untouched until this phase
reads out; it will be re-framed method-first afterward.

**Mode-2 outcome:** a generator whose one-octave extrapolated maps pass the
FULL audit — marginals, out-of-basis, and the joint/morphological peak tier —
so the paper's claim is "here is a method you can use for higher-order-
statistics mocks," demonstrated on the statistic Andreas's own field uses.

## Stage 0 — the free move (zero training; runs first)

Re-run the COMPLETE audit battery at the deployment edge and trained scales
with the FINAL SHIPPED configuration (heavy-tailed base + caged selection +
group-averaged symmetrized sampler): peak counts at all thresholds with the
bootstrapped CIs and per-parent panel, the spacing statistic, starlet-ℓ1,
marginal suite. The existing edge peak excess (+13.1/+14.6% at the decision
thresholds) predates the symmetrization, and the checkerboard was itself a
peak producer. Pre-register expectations with weights before scoring
(reconvene lines at review). Branches: S0-CLOSED (excess within the peak
audit's noise floor → jump to Stage 3), S0-SHRUNK (materially reduced but
open), S0-UNCHANGED. Every branch informative; no bar changes.

## Stage 1 — mechanism of the residual excess (descriptive, CPU)

Where do the extra peaks come from? Profile the excess: threshold dependence,
peak size/morphology (are surplus peaks small/grainy?), octave attribution
(hybrid-transplant style), and the spatial autocorrelation of generated vs
real detail coefficients (the colored-base motivation measured, not assumed).
Also: does the spacing residual co-locate with the excess?

## Stage 2 — the principled lever: the colored heavy-tailed base (single variable)

Extend the put-measured-structure-in-the-base doctrine: base noise with the
MEASURED spatial autocorrelation of real detail coefficients (per octave) and
the established heavy-tailed marginals (correlation via spectral coloring;
marginals via the existing t; copula-style composition — implementation
periphery, tests-first, exact-symmetry compliance preserved so the
group-averaged sampler's guarantee is untouched). Sandbox-first vs exact
truth; bars = full current suite MUST-NOT-REGRESS + peak-count excess primary;
gowerstreet leg conditional; prereg with weighted branches incl. gates.

## Stage 3 — method-paper hardening (whatever Stages 0–2 leave standing)

(a) Seed ensemble (n≥3) of the final recipe at production scale — the
robustness table a method paper needs; (b) the full blind-edge protocol
re-run once on the final configuration (one shot, pre-registered — the
method paper's headline table); (c) if the peak tier closes, promote a NEW
held-out judge never used in design (e.g., Minkowski functionals) so the
paper's validation keeps an untouched tier; (d) the isotropy/stationarity
declared-domain caveat (Andreas's question) into the method section.

## Discipline

House rules unchanged (prereg, weights incl. gates, bars from measured
references with #8–#11 lessons, numbers-by-copy, one variable, STOP at
readouts, ambiguity=negative). Budget: 10 H100-h cap (expect ≪). The paper
decision returns to Andreas at this phase's close with the method-first
framing as the target and the honest fallback stated now: if the tier will
not close, the paper's claims shrink to the statistics that DO pass — which
is still a usable-method paper for dispersion/tail-calibrated applications,
with the peak tier declared as the boundary.
