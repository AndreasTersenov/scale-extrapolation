# Appendix — Reproducibility and the audit of the audit

## A.1 The pre-registration ledger [WRITE: table]

Every experiment: prereg commit hash → run/job id → readout log → ruling
R1–R43. Source: log/ directory (preregs committed pre-run throughout; the
disclosed pre-readout corrections inline where they occurred: the G2
identity-gate repair, the pcure verdict-trigger correction, the sweep
per-parent pooling fix — each corrected blind or pre-adjudication and
disclosed in place). Config hashes verbatim from run logs.

## A.2 Data provenance and splits (accepted R27)

[Carried verbatim: gowerstreet 266/32/32 from 30 parents, parent-disjoint
test {27,28,29}, disclosed validation margins; sandbox seed streams;
HARDEN: tiles_pnull builder backfill.]

## A.3 The bar-design ledger (#8–#14, disclosed, verbatim from rulings)

#8 final-state statistics for decay questions; #9 reference-side SEs
budgeted (32-field references inflate 3·SE bars — disclosed BOTH ways in
§3.2); #10 exchangeable-unit bootstraps (parents not tiles; ν=1 never
headlined); #11 pre-declared ambiguity bands, disambiguation by replication
(and its one-shot exception: at-bar = the worse branch, no rerun); #12
per-statistic scope on comparative lines; #13 gate lines priced against the
literal rule AND the premise separately; #14 a branch carries a mechanism
meaning only if its rule measures that mechanism (the A2 lesson).
<!-- src: rulings R16–R43 -->

## A.4 Auditing the validators (instrument lessons)

- The starlet package findings (gen2 path; noise-plane periodicity) —
  reported upstream. <!-- src: starlet_l1_instrument.json -->
- The coloring index: the field-coloring validation synthetic FAILED
  pre-data (Haar high-pass ~k² cancels red P~k⁻²; decimation aliasing) —
  detail-space synthetics adopted; lesson: coloring statements about
  wavelet coefficients are made IN coefficient space.
  <!-- src: prereg-n1 validation note -->
- The Minkowski judge: the pure 4-connectivity Euler estimator read
  V2(0)=+0.010 on GRFs where symmetry demands 0 — the 4/8-connectivity
  average adopted PRE-FREEZE by the synthetic validation.
  <!-- src: minkowski_judge.py freeze notice -->
- The cross-run reproduction criterion for recursive generators:
  max-abs comparisons amplify per-op float noise multiplicatively through
  octave recursion; the corr/amplitude-ratio replay criterion (validated
  repeatedly at corr ≥ 0.9999999) is the standing convention.
  <!-- src: G2 execution note; R37.3 -->

## A.5 The scorecard (hits AND misses, both roles) [WRITE: table]

Full prediction table from R1–R43 including: the pre-phase-3 record
(carried); the phase-3 pattern — S0 both modals miss (UNCHANGED fires);
GATE-N1 both lines lose (bar-ledger #13 born); L1′ transfer line loses in
an unlisted direction; L1″ executor modal NULL hits on measured
dose-response where the mechanism hope missed; Stage-3 both modals miss
twice more (FRAGILE, PARTIAL), pessimistic tails closer; P-T 5/5 — the
one prediction engine that never missed. The four-of-five non-modal
pattern stated as the record's own argument for branch-complete preregs.
<!-- src: R43 scorecard section -->

## A.6 The quarantined dry-run (R43 finding 5)

The one-shot's pipeline dry-run on the committed stage-D lineage, outputs
quarantined from all adjudication: E2 −3.0/−3.0% (fail-side), T_MF
8.08/7.20. The fresh blind seed improved both — the headline pass is not a
lucky draw relative to the committed lineage. <!-- src:
stage3_dryrun_verdict.json (QUARANTINED flag) -->

## A.7 Test gates and environments

Both stacks green at every session end (Stop-hook enforced); the env split
(pywt/JAX process separation) noted as infra; the frozen-instrument
convention: every scorer imports committed instruments, never re-implements
them; A5 scoring-order enforcement structural (context-first files written
before adjudicating numbers exist).
