# §5 — The measured boundaries (declared, with mechanisms — R43 claim set)

Framing sentence [WRITE]: a method paper that prints its boundaries with
the same instruments that validated its claims; each boundary comes with a
located mechanism or a named open question, never a shrug.

## 5.1 Native-resolution peak counts (located mechanism)

- The number, with error bars and disclosures (both mandatory): the final
  configuration carries +14.8%±2.9 / +11.4%±2.6 native peak excess at the
  blind edge (ν=2.5/3.0; ν=1 rows never headlined, #10; 3-parent reference
  disclosure attached). <!-- src: stage3_blind_verdict.json supporting -->
- The located mechanism (§4.5 summary sentence): pixel-scale phase
  structure — independent of lattice symmetry, base coloring, and the
  octave-1 power spectrum at fixed weights; killed by 0.5-px smoothing;
  field-dependent in sign (the sandbox shows deficits — the sign-flip
  finding). All spectral inference-time levers are measured exhausted.
- Resolution scoping: the zero crossing of excess vs smoothing lies at
  0.28–0.54 px across configurations and legs
  <!-- src: l1pp_decision_sweep.json -->; standard WL peak analyses smooth
  at or above the declared σ_s ≥ 0.5 px — the boundary excludes a regime
  the field's analyses do not use, and says so plainly.

## 5.2 Minkowski morphology — the untouched judge's catch (the
## methodological thesis DEMONSTRATED; write as such per R43)

- The design: the judge (V0/V1/V2 at six thresholds, T = max|z|) was built
  tests-first, validated on synthetic GRFs only, committed FROZEN before
  any phase-3 lever ran, applied to NO generation until the one-shot, with
  its bar (3.5) frozen against a committed real split-half null
  (smoothed-domain 1.48 ± 0.55, max 2.87 over 20 seeded splits
  <!-- src: stage3_mf_null.json -->).
- The catch: T = 6.35 at the declared resolution (native 5.30) on the
  blind run — the one entry the final configuration fails, caught by the
  one tier no design decision ever optimized against, while five
  designed-against tiers passed. <!-- src: stage3_blind_verdict.json E3 -->
- The thesis sentence [WRITE]: validation suites for generative emulators
  need a held-out judge for the same reason blind analyses need blinding —
  and this paper demonstrates the practice on its own generator, at its
  own headline table, and prints the failure.
- Continuity: the morphology gap is continuous with the located pixel-
  scale phase mechanism (§4.5) — stated as the natural next-boundary
  hypothesis, NOT as a measured attribution.
- Appendix pointer: the quarantined dry-run showed the fresh blind seed
  IMPROVED both E2 and T_MF over the committed lineage (8.08 → 6.35) —
  the one-shot's marginal-and-peak pass is not a lucky seed (R43
  finding 5).

## 5.3 Seed-conditionality of the declared resolution (ordered wording)

R43 finding-3 wording, binding: the declared-resolution peak claim attaches
to the SELECTED configuration — the selection protocol is part of the
method — and the fresh-seed spread is printed in the robustness table
(§3.4: +3.6–6.6% at 0.5 px on fresh seeds whose caged picks landed early).
Candidate cure (late-window or matched-step selection) is drafted follow-up
and must not enter this paper's claims. The schedule mechanism (§4.4) is
the stated reason this is a selection property, not a lottery.

## 5.4 The remaining boundary rows, one line each

- Two-octave extrapolation degrades (−44/−54% kurt vs −29/−40 fully-
  trained reference) — one octave validated, the second octave's cost
  measured. <!-- src: stageD readout descriptive -->
- The spacing statistic: pooled multi-stream convention ONLY — committed
  single-stream values (T≈4) were stream maxima (white-stream spread
  4.045/3.245/2.148, sd 0.95 <!-- src: l1pp_decision_nn.json -->); pooled
  means sit at 2.6–3.2 across the final-config tables. The old "frontier"
  deflates to a stream-ensemble statement.
- Isotropy/stationarity: the §2.5 declared-domain caveat governs.
- The joint/morphological tier beyond counts (literal placement:
  environment-conditioned rates, clustering) remains untested — R27
  wording — pending the placement-experiment instruments (registered
  future work, §6).
