# 2026-07-25 — RECONVENE RULING R31: GATE-S adjudicated; the parity discovery adopted; Phase A′ ordered before any re-posed Phase B

## The gate

GATE-S fired exactly as pre-registered and the stop was correct: the sandbox
count deficit is NOT expressible in position-pure coordinates (primary nn
T=2.62 vs the truth-null band) — per the prereg's own meaning, the sandbox
tier-3 residual is texture-morphological, not placement. Scorecard: executor 8,
reconvene 6 — the low-weight-branches-keep-firing pattern continues post-R24
and is now its own calibration note (five instances). The razor-thin primary
qualification (T_surr 5.0028 vs 5.0, competitors at 4.70/4.31) was disclosed
unprompted — noted, and it softens any claim that nn was a POWERFUL instrument:
position-pure placement signal is intrinsically subtle in these fields.

## The discovery: lattice-parity bias — adopted as a first-class finding

The confound instrument found what nothing else could: generated fields
preferentially place peaks at odd-odd pixel parities (arm A 30.2% vs real
25.0%, z up to +9.9; the dial arm at T=20.8 — yet another instance of the dial
distorting joint structure). This is invisible to marginals, counts, AND the
starlet ℓ1 — a lattice-level morphological artifact, almost certainly tied to
the dyadic synthesis grid of the cascade. The audit's fourth distinct exhibit:
every tier of instrument so far has found a failure invisible to the tiers
below, and this one required an instrument built as a CONFOUND CHECK. Also
adopted: the real-field echo's mild but parent-consistent genuine nn signal
(T≈3.5–3.9, arm A) — the real field DOES carry a small position-pure placement
residual the sandbox lacks; the sign-flip story deepens.

## R31 orders

1. **Phase A′ (descriptive, committed artifacts ONLY, zero training):** localize
   the parity mechanism before any redesigned causal experiment. Measure parity
   statistics of the WAVELET DETAIL COEFFICIENTS directly (not just peaks):
   per octave, per detail channel (H/V/D), per arm, generated vs real; across
   the generator taxonomy (variance-head era, Gaussian-base, final recipe);
   across the checkpoint curve (does the caged selection influence it?); and on
   the Stage-D edge maps. Deliverable: the originating octave/channel and
   whether the bias is present in the coefficients or created at synthesis.
   Tests-first for the parity estimator; house rules throughout.
2. **Then the re-posed prereg** (parity as primary, nn/env descriptive, same
   causal arms + echo, re-registered weights) comes back for reconvene review —
   informed by A′, since if A′ pinpoints an architectural origin (a channel
   asymmetry at synthesis), the right experiment may be a FIX-and-remeasure,
   not a data-scaling arm. Reconvene lean, registered now to be scored: parity
   bias is architectural/synthesis-grid rather than data-limited — 55%.
3. **Paper note (within the session's authority when WP1 resumes):** the parity
   finding enters the discussion/frontier as the fourth exhibit with its CI
   panel; the razor-thin instrument disclosure accompanies any nn-based
   sentence.
