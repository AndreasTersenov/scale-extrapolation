# 2026-07-25 — ARM D READOUT: **D-SHRINK, at the bar (margin 0.0135) — and the
# seed rider it carried defeats the branch's own premise.** The defect at the
# caged pick is PLACEMENT-dominated, not data-dominated; data lowers the late
# plateau and (at 32×) opens a val-side joint window; F2-mode is clean at
# every scale. CUR-PARENTS fires on a same-pick comparison.

Prereg log/2026-07-25-prereg-parity-cure.md D block (R32 weights; R34
sequencing; execution reading documented pre-result in the F2 readout
append). Jobs: n1s1/n8/cens 17436879/80/82 COMPLETED (~21 min each); n32
17436881 OOM on MIG (10304-tile d4_augment on device) → full-H100 resubmit
17437389 COMPLETED (20:18); curves 17437678 same OOM → 17437897 COMPLETED
(11:29) — two gate resubmissions, both infra, logged. GATE-D never fired
(dispersion at truth everywhere). Artifacts: arms_pcure_{n1s1,n8,n32,cens}.npz,
pcure_selection_*.json, pcure_curves_gen.npz, pcure_verdict.json. Numbers
verbatim (R12).

## The table (model arm A; e = raw-mode T_coef oct-1 at the caged pick, vs
## the committed 1× baseline 15.3)

| arm | caged pick | e (raw) | corrHV at pick | joint pick | T_val at joint | F2-mode at pick |
|---|---|---|---|---|---|---|
| N1-seed1 (rider) | 5000 | 6.861 | +6.4 | 17500 | 3.49 | 1.03 |
| N8 | 10000 | **7.636** | +1.6 | 15000 | 4.73 | 1.32 |
| N32 | **1500** | **20.225** | +1.4 | 17500 | **2.88** | 1.11 |
| C-ENS | 10000 | 10.817 | +3.4 | 20000 | 3.34 | 1.12 |

## The mechanical branch: **D-SHRINK** — margin 0.0135

e(N8) = 7.6365 vs the frozen threshold 15.3/2 = 7.65: the ≥2× improvement
clause fires by 0.0135 (the third consecutive at-the-bar verdict: nn's
surrogate 5.0028, F2's 3.049, now this — noted for the bar-design ledger:
max-type statistics adjudicated against round bars keep landing at them).

**Stated with equal prominence — the branch's pre-registered MEANING does not
survive its own rider.** The seed rider (N1 retrained at seed 1, same data
size) lands at 6.861: BELOW N8. Against the committed cross-seed baseline
(15.3) N8 halves; against the seed-matched baseline it does not shrink at all
(ratio 0.90). The reference the frozen rule was written against carries ~2×
seed/placement variance, which is exactly what the rider was designed to
expose, and did. The D-SHRINK label is recorded; its "data helps" reading is
NOT claimed.

## The mechanism finding (the readout's substance)

**The defect at the caged pick is dominated by WHERE the cage lands on the
transient curve, at every data scale.** Exhibit N32: its cage picked step
1500 — an early val spike — where the defect reads 20.2, while its own late
curve reads 2.9 at 17500. More data did not rescue the estimand because the
estimand rides the pick, and the pick is blind to the defect. The
schedule-harvest mechanism (R32's named finding) is confirmed at three data
scales and is the binding variable — the branch table's implicit premise
(data moves the defect-at-pick) is the part reality declined.

**What data DOES do (val-side, descriptive):** the late-curve plateau falls
with data (N1 3.5 → N32 2.9 at 17500; C-ENS 3.3 at 20000) and corrHV at
comparable picks cleans (+6.4 → +1.6/+1.4); at N32 the joint criterion finds
the campaign's first sub-bar val reading (T = 2.88 @17500) — **the joint
window plausibly OPENS at 32×**, the D-WIDEN substance arriving through the
joint-selection door rather than the caged-pick door. Test-side confirmation
of that single point is one MIG-minutes inference pass (recommended to the
reconvene; not run — not in the frozen estimand set).

## Currency: **CUR-PARENTS** (adjudicating, conditional on D-SHRINK standing)

e(C-ENS)/e(N8) = 10.817/7.636 = 1.417 > 1.25, at the SAME caged pick step
(both 10000) — the cleanest row of the experiment: at matched total fields
and matched pick placement, independent parents beat constrained conditional
ensembles on the joint-structure defect. PM data-design consequence: budget
buys independent ICs first. (If the reconvene voids D-SHRINK's meaning, the
currency verdict reverts to descriptive; the same-step design keeps the
ratio itself trustworthy.)

## Production path (F2-mode, descriptive)

1.03 / 1.32 / 1.11 / 1.12 across all four arms — the group-averaged sampler
nulls the defect at every data scale. Shipping is unaffected by everything
above; the D question was always the mechanism chapter's, and it now has its
answer: the ladder-schedule harvest, not data starvation, governs this
defect's deployed magnitude.

## Scorecard (R32 registered weights)

D-WIDEN was modal for both parties (reconvene 45 / executor 40) — both MISS;
D-SHRINK fired at 25/25 — but the honest note is that the outcome that
actually occurred ("SHRINK by the letter, placement-dominated in substance,
window opening via the joint door") was on nobody's list: the ninth/tenth
unlisted-flavor outcome. CUR-PARENTS: reconvene 45 / executor 40 — both
modal-HIT. Seed rider: fired its purpose (baseline variance exposed);
N1-seed1 marginals passed their bars (seed-robustness evidence banked for
the paper's limitations section).

STOP (R34). The reconvene owns: the D-SHRINK reading vs the rider, the
currency verdict's status, the N32 joint-window test-side confirmation
(one cheap pass, recommended), and the paper's mechanism-chapter update.
