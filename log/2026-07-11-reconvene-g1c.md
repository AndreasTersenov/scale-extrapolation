# 2026-07-11 — reconvene ruling on G-1c (attempt-4 authorization, conditions binding)

## Verdicts accepted
G-1c FAILED as adjudicated; student-t correctly NOT fired (branch condition unmet).
The collapse law is hereby GENERALIZED and promoted: "finite-data memorization of the
conditional mean eats the conditional variance in whatever channel carries it" — three
independent channels, one mechanism, exact decomposition. This is now the centerpiece
of the methods finding (kill-test scope per addendum 3 stands, more urgent). Findings
promoted with it: exp-head OOD instability under extrapolated conditioning; GRF-null
sensitivity to the miscalibration (not non-Gaussianity-specific).

## Ruling: ONE further round authorized — it targets the diagnosed CAUSE, staged for attribution

- **Attempt 4a (run first, alone): symmetry augmentation only** (D4 group: flips +
  90° rotations, 8× effective data; nothing else changes vs the G-1c config).
  **This arm is simultaneously the TEST of the diagnosis:** pre-registered signature —
  the collapse onset (implied var_slope vs steps) must shift materially later (≳4×)
  if data-limited memorization is the cause. **If the collapse curve does not move:
  diagnosis refuted → HARD STOP, no 4b, reconvene** (D4 reshapes around break + law +
  validation architecture; the churn prototype's 90%-repair prior stays as an appendix
  result, not a claim).
- **Attempt 4b (only if 4a shifts the curve but the bar is still unmet): + two-stage
  frozen-mean residual-fit variance channel** (fit mean; freeze; fit variance on
  HELD-OUT residuals — the heteroscedastic-regression recipe), with validation-NLL
  early stopping. One escalation, then stop regardless.
- **Bars unchanged** (dispersion 1σ at octaves 2–4 simultaneously; kurtosis 2σ with
  student-t branch intact; GRF null INCLUDING the new bootstrap-consistency criterion;
  trained-octave amplitude undegraded). **New frozen requirement (finding 3): the
  variance channel's response to the conditioning coordinate must be bounded by design
  under out-of-range coordinates** — mechanism is periphery, requirement is core;
  demonstrate on the extrapolated octave of GRF.
- Predictions (Claude): P-4a-shift 65%; P-dispersion-bar after 4a/4b 55%; P-kurt-after
  50% (+student-t rescue ~15%).
- Process note (no blame, one line for RESULTS): estimator gates tested flip/rotation
  symmetry while training never used augmentation — add "training-data augmentation
  parity with estimator symmetry gates" to the portfolio checklist.

Budget: MIG-scale; well inside caps. Report and STOP at the 4a signature readout, at
any bar verdict, and before any P6 re-adjudication.
