# 2026-07-25 — ARM F2 READOUT: **F2-PARTIAL, at the bar** — the group average
# annihilates the defect everywhere except ONE component at ONE octave
# sitting 0.049 above the null bar. The nn spacing residual survives its
# third cure. Rider: corr(H,V) is ALSO a transient peaking at the caged pick.

Prereg log/2026-07-25-prereg-f2-groupavg.md (committed pre-run; R33 weights
inside). Jobs: 17424070 FAILED at arm B's first octave — the FiLM cond vector
was closed over at full batch size while the group-assigned assembly passes
per-element subsets; fix committed (cond sized to the subset — exact, the
vector is field-constant), resubmitted 17424336 COMPLETED (11:50). The one
prereg'd gate resubmission is consumed; the adjudicating arm A and the
identity gate were clean on BOTH runs (max abs diff 0.0). Artifacts:
f2_test_gen.npz, f2_test_hc.json, f2_verdict.json,
parity_ckpt_curve_full.json. Numbers verbatim (R12). D4 machinery
tests-first (tests_wfm/test_d4_action.py, 4 gates incl. exact symmetrization
of an injected mean defect).

## Verdict (sandbox arm A, frozen rules): **F2-PARTIAL**

| defect statistic | committed pick (A′/F) | F2 group-averaged | bar |
|---|---|---|---|
| T_coef octave 1 | 15.3 (F: 7.5) | **1.65** | 3.0 |
| T_coef octave 2 | 5.7 | **3.049** — the branch decider | 3.0 |
| parity level-1 T | 6.8 (F: 6.2) | **1.74** | 3.0 |

Marginals: ALL bars pass; every entry within 1.3σ of the F run (the
"unchanged within SE" texture holds — worst Δz 1.3, hc-oct3 kurtosis).
Arm B (descriptive): 1.7 / 1.2 / 1.9 — fully clean.

**The mechanical branch is F2-PARTIAL** (any defect stat ≥ 3; AMBIGUOUS=FAIL
is the standing asymmetry and 3.049 ≥ 3). **Stated with the same prominence:
the margin is 0.049 on a max-over-9-components statistic whose null
expectation is ≈ 2.5–3, driven by a single component — oct-2 meanD z = +3.05
— with the other eight components ≤ 1.7 and every oct-1/parity statistic
annihilated.** Under the pre-registered meaning, F2-PARTIAL asserts "a
D4-invariant component exists"; the evidence for that reading is one
at-the-bar component at 32 fields. Executor's recommended disambiguation
(reconvene authorizes): one MIG-minutes inference pass on the 64-field
replication stream — √2 more power on exactly this component. Until then the
letter of the rule governs.

## The corrected real-field echo (nn WATCHED)

| | before | F (constant) | F2 (group avg) |
|---|---|---|---|
| T_coef oct-1 | 7.5 | 4.6 | **2.7** |
| parity T | 2.5 | 1.6 | **1.3** |
| **nn T** | 3.5 | 4.1 | **4.0 — SURVIVES** |
| e2e oct-2 kurt (real 6.04) | 5.80 (−4.0%) | 6.40 (+6.0%) | **6.15 (+1.8%)** |
| e2e oct-2 var_slope (real 0.973) | 0.991 | 0.992 | 0.945 (−2.9%) |

The watched line resolves as registered (P(nn moves below 3) = 10 — it did
not): the spacing residual is invariant to the symmetry cure, as D4-invariance
of nn distances predicts. **The tier-3 frontier statistic has now survived
three cures (constant, joint-selection reasoning, exact symmetrization) — it
is real, small (T≈4), parent-consistent, and unexplained.** F2's echo texture
dominates F's on every other line.

## Rider (R33 order 2): the corr(H,V) layer across the M5 curve

Arm A: −1.2 → +5.2 → **+6.9 @7500 (the caged pick)** → −0.0 → +1.0 → +3.9 →
+2.3 → +1.8 @20k. The cross-channel layer is ALSO an optimization transient
peaking at the marginal-optimal checkpoint — both defect layers are
schedule-harvested, none is a fixed property of the architecture. (Arm B's
corrHV stays ≤ |3.9| throughout; its T is mean-driven.)

## Scorecard

F2-CLEAN: reconvene 70 MISS, executor 65 MISS — both priced the theorem and
under-priced the one-component boundary; F2-PARTIAL fired at reconvene 15 /
executor 20 (executor closer). The margin note above is the honest content:
substantively the group average performed as the invariance argument
predicts; the record keeps the letter of the bar (the A-hc discipline).

## Adoption recommendation (reconvene's call, per the prereg)

F2 dominates F on every defect statistic and on the echo texture, costs the
same per sample, and carries no fitted constants (nothing to go stale under
retraining). Recommend: F2 ships as the generator's calibration layer;
F-const remains the paper's ablation exhibit (the two-layer decomposition it
revealed is the mechanism figure). D proceeds per R32/R33 unchanged, with
oct-2 meanD and corrHV added to the per-arm watch list. STOP.

---

## DISAMBIGUATION READOUT (R34 order 1; appended 2026-07-25; job 17436655,
## 4:29; verbatim from results_p2/f2_disambig_verdict.json)

On the 64-field replication stream (seed 20260720, disjoint), F2 arm A @7500:
**oct-2 meanD z = −0.01** (the 32-field decider was +3.05); oct-2 T = 1.68,
oct-1 T = 1.98, parity T = 1.36 — every statistic null-consistent at √2 more
power. Per the pre-stated R34 reading rule: **consistent-with-null — the
at-the-bar component was a 32-field fluctuation; recorded, no retro-upgrade
of the F2-PARTIAL branch label.** Substance for the paper: the group-averaged
sampler leaves NO detectable D4 defect at 64 fields; the branch label and
this disambiguation travel together (the bar-honesty discipline, both
directions).

## Arm D execution note (interpretation documented BEFORE any D result)

R34 orders D "as approved under R32, with the corrected+symmetrized sampler
as substrate." Read literally, F2-mode sampling would null D's primary
estimand (T_coef) by construction and destroy comparability with the
committed 1× baseline (15.3, raw mode). Adopted execution reading, within
the estimators-and-implementation periphery: the four data arms run the
UNMODIFIED committed runner (raw-mode estimands — the defect lives in the
model; D asks whether DATA removes it, F2 masks it at sampling), and the
F2-mode production texture is a separate inference pass at each arm's caged
pick (descriptive). The frozen estimand, bars, and R32 weights are untouched.
