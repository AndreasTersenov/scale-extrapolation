# 2026-07-24 — RECONVENE RULING R30: placement prereg APPROVED; skeleton compliance clean; the next gate is Andreas's

## WP2 — the placement prereg: APPROVED as drafted

Review verdict: this is the best-constructed prereg of the campaign. Singled
out for the record: the phase-randomized surrogate gate (rank-remapped
marginals, joint structure destroyed — the exact "can this instrument see pure
joint damage" test); the mechanically Goodhart-caged primary-instrument rule
(truth-side properties only, committed before any model scoring); GATE-S's
null being a finding; the branch table where "no branch confirms-without-
teaching; no branch is uninterpretable" is actually true; the transfer clause
binding on every readout sentence; N1 doubling as the fresh-seed replication
rider without adjudicating. Ledgers #8/#9/#10 enforced by construction.

**Reconvene weights, registered now against the branch table** (executor's in
parentheses): GATE-I 3 (4) · GATE-S 6 (8) · GATE-D 2 (3) · PL-DATA 38 (33) ·
PL-SAT 15 (14) · PL-WEAK 20 (19) · PL-FLAT 16 (19). Sum 100. PL-SELECTION
fires: 20 (15) — the cage was tuned on marginals and the audit's own logic
says marginal-optimal ≠ joint-optimal is live. Currency, conditional:
CUR-PARENTS 45 (40) · CUR-ENSEMBLE 20 (25) · CUR-EQUIV 35 (35). Rationale for
the PL-DATA lean: same mechanism class as rungs 2/4, both of which moved hard
under the same lever; tempered by the sign-flip novelty.

**GO: Phase A may start immediately** (CPU only). Per the prereg's own rule,
Phase B submits after the Phase-A readout appends the PENDING references —
no further reconvene paste required unless a gate fires; I audit at the
readout as standing.

## WP1 — skeleton compliance: clean on spot-check; the structural read is Andreas's

Verified present: Gate-0 claim wordings with scope clause (§4, verbatim);
the structured-not-additive flagship placed (§4.2); the A-hc 0.1%-margin
statement (§5); the 3-parent disclosure + R27 morphological wording (§5, §6);
the sign-flip finding + transfer clause (§6); ledger #8–#10 + split provenance
(§7); Schanz prior-art positioning (§3). The restructure follows the audit's
3.1 plan. The reconvene does NOT pre-empt the structural read: section
ordering, narrative choices, title selection, and the venue call are Andreas's
gate, with the session's astro-first recommendation and the reconvene's
concurrence on file (R28).
