# 2026-07-17 — R13 OBJECTIVE BAKE-OFF: spec, pinned candidates, executor weights
# (committed BEFORE any bake-off result exists; R12 verbatim-copy rule in force)

Authorization: log/2026-07-17-reconvene-c3blocker.md R13. Three candidates through
the SAME validated gates that fired the C3 blocker — the t(5) flat-σ field toy and
the modulated-σ × t(5) composite — production net (32,64,128)/bottleneck 256/embed
128, production patch config where applicable, 768 train / 256 held-out fields,
4k steps, lr 1e-3, seed 2, held-out eval, resid_stats(skew, excess kurt, q999).
Runner: scripts_p2/bakeoff_c3.py; artifacts results_p2/bakeoff_<cand>_<toy>.json.
Reference points from the blocker (verbatim, committed jobouts): β=1 fails at
0.49/6.0 (t5) and 1.10/5.96 (composite); exp control passed at 5.76/6.0.

## Pinned candidates

1. **twcrps** — patched ES on chain-transformed coefficients, v(z) = z +
   a·sign(z)·max(|z|−τ, 0), τ=2, a=4 (arms_p2/c3/energy.chain_tail). Rationale for
   the pins, stated before results: τ=2 because the measured β=1 deficit lives
   beyond ~2 standardized units (bulk moments were calibrated; truth q999 range
   4.6–6.0); a=4 gives 5× tail weight while piecewise-LINEAR chaining keeps score
   terms O(|X|) so gradient variance stays finite for t(5) targets (a polynomial
   chaining would need E|X|⁶ = ∞). Kernel-score identity validated against direct
   twCRPS quadrature in tests_p2/test_bakeoff_candidates.py. β=1, m=8, patch 8/4.
2. **beta05** — patched ES with β=0.5, all else the frozen C3 objective. The cheap
   in-family probe R13 ordered; executor's stated prior is that the second-order
   structure is in the score's FORM, not β.
3. **tbase** — C1 CFM verbatim with unit-variance Student-t(5) base and heun-80
   pushforward (arms_p2/c1t/flow.py). ν=5 pinned for the toys (matches the gate
   target's tail order). If this wins it runs as a NEW arm C1-t with its own
   prereg (R13 note) — and that prereg must pin the sandbox ν from stated
   reasoning before results.

## Selector (copied from R13, verbatim)

"a candidate QUALIFIES if held-out t(5) excess kurtosis ≥ 4.0 (of 6.0) AND
composite pooled delivery ≥ 4.0 (of 5.96) — two-thirds recovery, set from the
measured fail (0.49) / pass (5.76) separation. Priority if several qualify:
twCRPS > β-variant > t-base (objective-family continuity first)."

Conditional pre-approval per R13: the qualifying candidate proceeds to the GPU leg
with (a) this verdict table committed, (b) the prereg amendment (or new C1-t
prereg) with re-registered branch weights committed BEFORE submission, (c) bars
and R10 conditions 1–2 unchanged. No qualifier → STOP.

## Registered weights

Reconvene (from R13): P(twcrps qualifies) 55; P(beta05) 25; P(tbase) 60;
P(≥1) 85; P(GPU head-conditional kurtosis bar | qualified) 55.
**Executor, registered now:** P(twcrps qualifies) **60** (the chaining makes tail
mass first-order by construction; main risk is bulk/tail trade-off distortion at
a=4); P(beta05) **15** (form-not-β mechanism read); P(tbase) **55** (base tail
order survives the pushforward in principle, but this family's measured history
is ODE under-dispersion of learned structure — the flow may Gaussianize the
realized tails it doesn't need for L2); P(≥1 qualifies) **85**; P(GPU
head-conditional kurtosis bar | qualified) **50**.

## Verdict table (filled by verbatim copy from results_p2/bakeoff_*.json;
## jobs 16609897/98/99/900/901/902, all COMPLETED 6–21 min)

Selector bars: t5flat final kurt ≥ 4.0 AND composite final kurt ≥ 4.0 (absolute,
adjudicated at the pinned 4k-step readout).

| candidate | toy | final skew | final kurt | final q999 | bar | verdict |
|---|---|---|---|---|---|---|
| twcrps | t5flat | +0.512 | 3.01 | 5.08 | ≥4.0 | FAIL |
| twcrps | composite | +0.519 | 3.57 | 5.26 | ≥4.0 | FAIL |
| beta05 | t5flat | +0.042 | 0.51 | 3.71 | ≥4.0 | FAIL |
| beta05 | composite | +0.063 | 1.05 | 4.03 | ≥4.0 | FAIL |
| tbase | t5flat | +0.016 | 3.56 | 5.10 | ≥4.0 | FAIL |
| tbase | composite | +0.007 | 2.98 | 4.98 | ≥4.0 | FAIL |

**NO CANDIDATE QUALIFIES → STOP (R13).** No GPU leg, no prereg amendment.

Kurt trajectories (verbatim from the jsons), for the reconvene's reading:

- twcrps t5flat: 500:2.69 1000:3.35 1500:2.51 2000:**5.14** 2500:4.09 3000:3.15
  3500:2.99 4000:3.01; composite: 500:3.18 1000:3.61 1500:4.03 2000:**4.80**
  2500:4.34 3000:3.58 3500:3.56 4000:3.57.
- beta05 t5flat: 500:1.26 1000:1.08 1500:0.70 2000:0.52 2500:0.55 3000:0.51
  3500:0.51 4000:0.51; composite: 500:2.22 1000:1.65 1500:1.31 2000:1.10
  2500:1.05 3000:1.05 3500:1.04 4000:1.05.
- tbase t5flat: 1000:3.63 2000:3.76 3000:**4.11** 4000:3.56; composite:
  1000:4.34 2000:**5.13** 3000:3.63 4000:2.98.

Texture the mechanical verdict does not capture (facts, not advocacy):

1. **twcrps and tbase both CROSS the bar mid-training** (5.14/4.80 @2k; 4.11 @3k /
   5.13 @2k) and decay by the pinned readout. A different stopping rule would have
   qualified both — but the readout step was pinned before results, and
   revisiting it now would be selector shopping; the campaign's collapse history
   (running-peak rule) is the cautionary precedent for trusting mid-training
   peaks. The reconvene owns any checkpoint-rule variant.
2. **twcrps induces spurious skewness ~+0.51 on a SYMMETRIC target, in BOTH
   toys** — the a=4 chaining distorts third moments while helping fourth; a
   disqualifying pathology beyond the bar number.
3. **tbase confirms the registered Gaussianization concern** (executor weight
   note: "the flow may Gaussianize the realized tails it doesn't need for L2"):
   composite 5.13 @2k → 2.98 @4k, monotone late decline.
4. Eval-noise note: the t5flat held-out DATA reference at this seed measures kurt
   4.15 (n≈196k of a t(5); the finite-sample estimator sits below the population
   6.0) — the ≥4.0 bar is nearly at the data's own realized value, so the t5flat
   leg was strict at this seed. The composite reference measured 5.96. Stated for
   calibration of the bars if the reconvene re-runs any variant; changes nothing
   tonight.

## Prediction scorecard (bake-off)

P(≥1 qualifies): reconvene 85, executor 85 — **both MISS** (modal expectation
failed). twcrps: reconvene 55 / executor 60 — miss. tbase: 60 / 55 — miss.
beta05: 25 / 15 — hit (correctly low). The shared miss direction: both panels
priced "some tail-weighted variant works at toy scale" too high; the measured
lesson is that BOTH proper-score reweighting and base-tail injection reach the
right tail mass transiently and then converge away from it under continued
training — the stable-recovery problem is harder than the reach-it-once problem.
