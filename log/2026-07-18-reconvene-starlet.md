# 2026-07-18 — RECONVENE RULING: starlet-ℓ1 readout adjudicated — SL1-PASS stands; the tier claim is now demonstrated with the field's own instrument

R26. Verified from raw JSONs (edge arm A 5.4/4.0/3.0/1.8%; B 2px +23.9%; secondary
convention robust). Rulings:
1. **SL1-PASS stands on all three legs, arm A, every scored scale** — including
   the Stage-D edge at +4.0% vs the 10% floor: the extrapolated maps carry the
   field's most constraining statistic. The truth-referenced sandbox leg (0.0–1.1%)
   is the strongest validation this statistic has likely ever received.
2. **The tier question FIRED as pre-registered (70% — hit):** position-blind
   starlet-ℓ1 passes exactly where the position-aware peak audit fails (+13/+15%).
   The paper's §4 sentence is earned verbatim; the honest nuance (the 4px tail
   share sees a +5.9% sub-bar hint of the excess) is quoted alongside. Tier
   ordering unrevised.
3. **Arm B's off-binding pathologies reappear in the held-out basis** (sandbox 2px
   −11.4%; edge 2px +23.9%, noise-dominated away under survey convention) —
   independent confirmation of R19's arm-A-reference ruling.
4. **The taxonomy panel is adopted as a paper figure:** the ℓ1 separates the
   diseases (NLL-head graininess = +14.2% amplitude with COLLAPSED tail share;
   μ-only skeleton −84%; C1 vs C1-t below total-ℓ1 resolution, ranked only by the
   4px tail share in the kurtosis-established order — a statistic-resolution
   observation worth one sentence).
5. **Instrument findings go UPSTREAM:** the two wl_stats_torch findings (gen2
   reconstruct mismatch on the unused path; zero-padded noise plane breaking
   periodicity/D4 of shipped SNR, with the adopted interior-plateau convention
   matching the analytic B3 table) are Andreas's to patch in his package —
   drafted as issues, his call.
6. Scorecard: P-SL1-trained 65 HIT, P-SL1-edge 55 HIT, P-SL1-blind 70 HIT —
   first clean sweep of a readout's full prediction table, both roles. The
   re-anchored (post-R24) priors are calibrating correctly on constructive
   outcomes.
Outstanding queue: the PAPER SKELETON (SPEC-paper-skeleton.md) — now the only
open task before writing.
