# 2026-07-18 — RECONVENE RULING: tail-dynamics adjudicated — moment-ladder confirmed at rung 4; arm C1-t authorized

## R16 — adjudication of the R15 diagnosis

1. **P1: the mechanical verdict TD-PARTIAL stands** (rule as written; one of two
   candidates ≥2× on the primary gate). Scorecard under the registered rule:
   executor modal TD-SHIFT 50 MISS (15-branch fired); reconvene 60 MISS — scored as
   a miss with no intent-credit, same treatment the executor's 4a literal-rule got.
   **Bar-design ledger #8:** onset rules read transient dips as decay onsets and
   cannot distinguish dip-and-recover from terminal collapse; decay questions get
   final-state statistics from now on.
2. **The final-state evidence is ADOPTED as the substantive finding:** every 1× run
   terminally collapses (kurt 1.1–1.7); at 8×, three of four runs sit AT truth
   (6.11/5.96, 4.32/4.15, 4.57/4.15) and hold flat for thousands of steps; the
   fourth holds at 80%. Within this window the 8× decay is largely ABSENT, not
   delayed. **The moment-ladder hypothesis is confirmed at rung 4 by the same
   causal lever that cured rung 2.** Together with N_eff ≈ parents, the mechanism
   statement for the paper: finite-parent training data starves conditional
   structure moment by moment; each rung has a causal data-size signature; rung 4
   needs more effective data than rung 2 (rare events live in parents).
3. **P3 is the mechanism gem and is adopted verbatim:** at 1× the trained flow
   converges to BASE-INDEPENDENCE (t-base and N-base outputs converge to the same
   light-tailed endpoint — the map memorizes a fixed conditional regardless of what
   enters); at 8× the base's tail contribution survives training with a stable gap.
   Base erasure is a data-starvation symptom, not a property of the flow — the
   cleanest mechanistic bridge between the moment-ladder and effective data size.
4. **twCRPS is RETIRED from candidacy** on two independent grounds: bake-off
   non-qualification and the skew pathology that WORSENS with data (+0.79…+1.11 at
   8×) while the t-base stays clean (+0.014…+0.023). The a=4 chaining distorts
   third moments structurally; no re-entry without a redesigned weighting.

## R17 — AUTHORIZED: arm C1-t — t-base CFM + pre-registered validation-selected early stop

Design cores (executor periphery elsewhere):
- **Arm = C1 architecture with Student-t(ν=5, fixed) base + a pre-registered
  checkpoint-selection rule.** Selection is part of the arm by design (P2: joint
  viable windows exist at 1×; at the real-data regime — more starved than toy 1× —
  selection is the load-bearing lever). Goodhart cage: the selection rule
  (criterion and window) is fixed in the prereg, computed on VALIDATION fields
  only; adjudication on the held-out set; one selected checkpoint, no re-picks.
- **Attribution at zero cost:** score C1's EXISTING sandbox checkpoints for tails
  (descriptive) so base-effect and selection-effect separate without new training.
- **Legs and bars unchanged from the approved C3 frame:** sandbox first at the
  phase-1-mirror regime (322 tiles + 8× augmentation); kurtosis PRIMARY
  (max(15%, 3·SE_rel), octaves 2–4, same-convention truth); dispersion
  must-not-regress vs C1; tail_q999 descriptive; light sampler sanity check for the
  t-base ODE at field scale (the bake-off validated it at toy scale only);
  gowerstreet leg triggers on the sandbox pass, descriptive readout, reconvene
  adjudicates. Prereg with weights incl. gate branches, committed before
  submission. GPU: MIG-minutes.
- **Reconvene weights, registered now:** P(sandbox kurtosis bar passes at the
  selected checkpoint) 55%; P(joint viable window exists at the real-arm regime)
  60%; P(gowerstreet e2e kurtosis deficit halves vs C1) 50%; P(dispersion regresses
  — the selection picks an early, under-trained dispersion state) 20%.

## Consequences ledger

If C1-t passes: the constructive arc closes — collapse cured (rung 2, data),
mixture artifact removed (head retired), tails restored (base + selection), and
the remaining gap becomes the extrapolation question Stage D was always about,
now with the re-scoped dial question. If it fails: the moment-ladder paper gains
its third tested-negative with mechanism attached, and the data-investment thesis
(more parents; constrained ensembles at UVA) becomes the program's spine — already
supported at two rungs by causal tests. The paper decision (deferred at Andreas's
rescue-first call) is ripe at this arm's readout either way.
