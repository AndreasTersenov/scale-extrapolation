# 2026-07-16 — gallery render log (SPEC-gallery.md executed)

One script (`scripts/plot_gallery.py`), four figures, FROZEN artifacts only — no
training, no new measurements, no new claims. All quoted statistics come verbatim from
the committed score jsons (`arms_aug_score.json`, `arms_selfsim_score.json`,
`downstream_peaks.json`) and `running_couplings.json`.

## Provenance (honesty rules 1 & 5)

- **Field index: 0** (first held-out) in every single-field panel, declared in the
  SPEC before rendering; no index substitutions were needed (no NaNs).
- **FIG-G1/G2/G3 use the committed run outputs** — `results/arms_aug.npz` (4a frozen,
  config_hash 1e61bd812a, generation key PRNGKey(1) from that run) and
  `results/arms_selfsim.npz` (control, config_hash 3c03bded83, same key convention).
  G1's intermediate rungs are recovered by Haar-decomposing the committed generated
  field (the recursion is exactly invertible) — no re-sampling.
- **FIG-G4 draws nine NEW conditional samples** from the frozen 4a arm-A checkpoint
  (`data_cache/ckpt_aug/armA_gowerstreet.pkl`), keys PRNGKey(1)…PRNGKey(9), from the
  index-0 real octave-4 coarse.
- Arms displayed: G1 arm B (the coupling dial is its conditioning); G2/G3/G4 arm A
  (stats quoted match the displayed arm; G3's inset shows both arms).

## Color scales (rule 2)

G1: shared per row (real vs generated at the same octave). G2: shared per row,
symmetric ±99th percentile of the pooled pair, RdBu_r (signed detail band).
G3: one shared inferno scale across both maps. G4: one shared scale across all ten
panels (real + nine samples).

## Deviations / decisions

- G2 optional residual column: DROPPED per the SPEC's own out-clause (conditional
  samples differ legitimately; a residual map invites exactly the pixel-match
  misreading rule 3 forbids).
- G3 1.0–1.5σ markers: drawn on the generated map only (SPEC wording), since the
  measured count differs visibly (777 vs 590 per map at ν=1).
- G1 caption bug caught at review before commit: the dial/statistic attribution was
  off by one octave in the first render (the map at octave j is produced by sampling
  octave j+1's detail with coords[j+1]); fixed so every rung quotes the dial and the
  measured var_slope of the octave actually sampled at that step.

## Outputs

`results/gallery_ladder.png`, `results/gallery_boundary.png`,
`results/gallery_peaks.png`, `results/gallery_ensemble.png`.
