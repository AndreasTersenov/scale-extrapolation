# 2026-08-06 — RECONVENE R48: night-3 harvest — the truth bug adjudicated
# (R43 finding-3 and R47's O-branch AMENDED); three levers closed with
# mechanisms; D1 corrected-selection re-ship AUTHORIZED; D2 + conformal
# preregs ordered

Ruled on: NIGHT-REPORT-3.md (cf7d6eb) + the night's full commit chain
(d525f7d → cf7d6eb) + artifacts. Same-day git log; one queue slot (unrelated).

## Verification

R12 spot-checks ALL MATCH: cage recheck (reference kurtosis 7.2738;
oracle @5000→@16000 with the shipped pick rescoring 2.08 = rejected;
seed1 @3500→@19500 / 1.92; seed2 @5500→@16000 / 1.69); oraclefix (MF_dev
declared 3.866, native peaks +15.22/+19.12, C 0.7734 in band, exec
expectation booleans recorded in-artifact); AUG verdicts (MF 9.34/9.09,
peaks −15.5/−13.1 and −15.1/−14.2, C 0.9672 out-of-band / 0.7678 in,
branch AUG-NULL both). Suites re-run at harvest: tests/ 62 pass (the
night added ~14 tests); tests_wfm+tests_p2 pass (exit 0). Blind-amendment
timeline audited: A-N3-2 committed before the sweep was read; CASC run-1
invalidation and its licensed repair pre-stated before resubmission;
DL-8's declined improvisation slot CREDITED (re-ship decisions are
reconvene territory — exactly right).

## The truth bug — formal record corrections

The disclosed `--truth` omission (selection cage referenced SANDBOX
kurtosis 4.917 instead of gowerstreet 7.274) contaminated three committed
trainings' checkpoint selections. Corrections, binding:

1. **R43 finding 3 (seed-conditionality of the declared resolution) is
   AMENDED:** the "fresh seeds pick early and fail" evidence was
   substantially a mis-referenced selection rule — under the correct cage
   all seeds' picks cluster late (@16000/@19500/@16000; AUG's @27000).
   The finding's final form awaits D1's re-scored battery; the paper's
   05-boundaries seed-conditionality wording is QUEUED for revision after
   D1 (paper remains paused). Both selections stay in the record for the
   calibration appendix.
2. **R47's O-branch (O-NULL as measured) is VOIDED-as-measured:** the
   oracle's checkpoint was chosen by a broken rule. The corrected reading
   (adopted): topology at the oracle ≈ selection artifact + checkpoint
   texture noise (3.87 at the corrected pick, pass-category e2e
   checkpoints exist at 2.89); **the native peak excess is the genuine
   model-class residue** (+14–22% at all 24 sweep points, both bugged and
   corrected oracle picks) — no selection and no oct-1 training moves it.
3. **Ledger #17 (instrument):** every selection/scoring job must assert
   its reference identity IN-ARTIFACT (reference values recorded and
   checked at runtime — the recheck artifact's `reference` block is now
   the mandatory pattern). Corollary for calibration (#17b): before
   weighing branches on an instrumented experiment, VERIFY THE APPARATUS
   — R47's O-FIXES 45 was wrong for a reason no branch table could price.

## Lever closures (all three, with mechanisms — adopted)

- **AUG RETIRED:** band-limited-safe degradations teach a rolled-off
  finest-band law (under-peaked, fragmenting; the sign-flip to −15% and
  the first P-T band miss in eight chains are its signature). The failure
  is intrinsic to the family, not tuning. The ×2 no-op proof (Haar
  nesting) is a nice negative exhibit for the paper.
- **CASC-at-inference DEAD:** seed arrangement does not survive fixed
  weights (max |Δ| 2.72; run-1 explosion correctly voided). With L1″:
  the flow's output texture is a property of WEIGHTS — corrections act on
  weights (training) or outputs (corrector), never on inputs. Doctrine.
- **TIDAL DESCOPED at depth 1** (GATE-T z=2.67 sub-bar; machinery built,
  proven, shelved) — and the depth axis is now the adopted organizing
  fact: decoherence z=2.67@depth-1 vs 4.50@depth-2, blind 0/8 at every
  checkpoint, BL's ladder concordant. **Every texture pathology scales
  with extrapolation depth; none with data availability alone.**

## Scorecard (both columns, full honesty)

Executor swept the night's registered lines: GATE-T (40 vs my 60), CASC
(22 vs my 30), AUG-NULL (47 vs my 30) — three straight where my
constructive lean lost to their mechanism-grounded skepticism. Their one
miss: O-CORRECTED MF ≤ 3.5 at 60 (3.87). **Calibration amendment #18: my
lever priors start from skepticism unless a measured dose-response
exists** — phase-2 cures went 5-for-5 WITH dose-responses; phase-3b
levers went 0-for-3 on mechanism stories alone. The distinction is the
predictor, not optimism vs pessimism.

## Orders

1. **D1 AUTHORIZED (the truth-bug remediation; runs now, ~0.2 H100-h):**
   re-select oracle/seed1/seed2 at the corrected picks (zero training),
   re-run the three final-config chains + FULL battery (+ the
   constraint-tier-0 violation instrument from BRIEF-foundations as a
   free new battery row), re-state the stage-3 seed-ensemble and O-arm
   conclusions with corrected picks alongside the bugged originals.
   Prereg with weights first (my lines: P(all three corrected seeds pass
   the declared-resolution peak rule) = 60 — the bug explains the old
   failures but ckpt texture noise is real; P(corrected-oracle MF_dev
   ≤ 3.5 on finals) = 35 — 3.87 stands until moved; P(peaks persist
   +10%+ on all three) = 85).
2. **D2 PREREG ordered (not run):** the second blind shot on the shipped
   post-D1 configuration, JUDGE-2 (persistent homology) as the unburned
   tier, A4-pattern committed null bar, one shot. Comes to reconvene
   review with the D1 readout. Judge economy holds: NOTHING touches
   JUDGE-2 before that shot.
3. **CONFORMAL PREREG ordered (drafted with D2, per BRIEF-foundations
   #2):** the rank-uniformity certification program — n≈50 fresh
   held-out coarse fields disjoint from training/selection/blind sets,
   m generated completions each, rank of the true statistic within the
   generated ensemble for peak bins + starlet scales; run identically on
   the corrected oracle for the certified extrapolation-cost band. Burns
   no judge; becomes the paper's certificate machinery.
4. **Retired/parked:** AUG family retired; D3 (TIDAL depth-2) parked with
   its machinery; D4 (cascade training) parked at lowered prior; D5
   (corrector) still draft-only. D6 noted as drafted-only: a
   texture-aware cage (selection on MF_dev at declared resolution) — the
   "selection cannot see texture" residual's legitimate lever; requires
   its own prereg + Goodhart discussion; NOT part of D1 (one variable).
5. The W2 theorem + negative lemma (BRIEF now-tier) are assigned to the
   next WRITING session (paper still paused; the theorem is paper
   machinery, not an experiment).

Budget: night 0.51 H100-h; phase ≈2.6 of 10. STOP at the D1 readout +
D2/conformal preregs for reconvene review.
