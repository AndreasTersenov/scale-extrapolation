# 2026-07-25 — RECONVENE RULING R33: F-PARTIAL and J-TRADEOFF adjudicated; the defect has two layers; arm F2 (exact symmetrization) ordered before D

## Adjudications

1. **F-PARTIAL stands.** The constant correction halves the coefficient defect
   (T 15.3→7.5) at ZERO marginal cost (no bar moves — F-BREAKS never
   threatened), and the residual is now understood: **the defect decomposes
   into a MEAN layer (cured by the constant) and a CROSS-CHANNEL layer —
   corr(H,V) = +7.5σ — that no constant can fix.** The correction is adopted
   as a permanent free calibration layer of the generator regardless of what
   follows. Scorecard: reconvene F-CLEAN 55 MISS; executor modal F-CLEAN 45
   also missed; executor's F-PARTIAL 30 closer than my 22. Seventh
   unlisted-or-underweighted outcome; the theme continues.
2. **J-TRADEOFF stands, and the schedule bind is now demonstrated
   mechanically:** the joint-optimal checkpoint (16000) fails the tail bar at
   −23.9% vs 15% — at 1× data, tails decay before symmetry cleans and NO
   single checkpoint satisfies both tiers. Both parties' modal weights were
   right (50/55). The paper sentence is secured with a number on it.
3. **The corrected real-field echo delivers the quiet headline:** parity is
   essentially cured on the real field (T 2.5→1.6), the coefficient defect
   drops to 4.6 — and **the nearest-neighbor spacing residual SURVIVES
   (3.5→4.1)**. The real field's genuine position-pure placement signal is
   INDEPENDENT of the parity defect. That residual — small, parent-consistent,
   surviving every cure so far — is now the true tier-3 frontier and is named
   as such for the paper.

## R33 orders

1. **Arm F2 — the group-averaged sampler (inference-only; quick prereg then
   GO):** restore the symmetry EXACTLY instead of correcting its violations
   one layer at a time. Sample as d = g⁻¹·model(g·c) with g drawn uniformly
   from the D4 group per draw: the sampling ensemble becomes equivariant BY
   CONSTRUCTION, which kills the mean layer, the cross-channel correlation,
   AND the pixel-parity preference simultaneously (each defect statistic is
   sign-moved by some group element, so the group average nulls it), while
   every D4-invariant statistic — all marginal bars — is untouched in
   distribution. Same cost per sample. Prereg with weights (reconvene,
   registered now: F2-CLEAN — all D4-defect statistics < 3 with marginals
   unchanged within SE — 70; F2-PARTIAL — improvement short of null, meaning
   the defect is not purely an equivariance violation — 15; gates 15), K-fold
   conventions as in F, corrected real-field echo re-scored (nn watched).
2. **Descriptive, zero cost, alongside:** corr(H,V) across the existing M5
   checkpoint curve — is the second layer also an optimization transient?
   Informs D's reading either way.
3. **D proceeds as approved (R32 weights unchanged)** after F2's readout: the
   schedule-bind question (does data merge the windows?) and the currency arm
   are untouched by F2, and the surviving nn residual gives D's echo leg a
   sharper target.
