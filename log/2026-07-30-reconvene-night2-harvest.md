# 2026-07-30 — RECONVENE R38: NIGHT-ORDERS-2 harvest — gate discipline
# exemplary; M-GRAIN adopted as leading mechanism; L1 (one-octave colored
# base) AUTHORIZED with a transfer rider; L3 authorized in parallel;
# Minkowski judge frozen BEFORE any lever run

Ruled on: NIGHT-REPORT-2.md (045051a) + the night's full commit chain
(5e955b4 → 3a1a158) + artifacts. Same-day git log taken; queue empty.

## Verification

1. **R12:** sampled verbatim across all five artifact families —
   gate/coloring table (stage1_p3_probes.json: oct1 trained C_real
   0.7864±0.0100 / C_gen 0.7237±0.0072 / z 5.09; oct2 1.35; oct3 −1.00;
   edge 5.38/2.81/0.15), morphology heights (+14.48±3.12 at ν2.5 …
   +7.2±4.9 at ν4), smoothing (−1.7±2.3 and −2.1±2.7 at 0.5 px; −8.2/−10.4
   at 1 px), transplants (oct1 −5.17±2.67 / −16.64±2.15), hybrid nn
   (4.62/5.16/2.00/0.92; full-gen 4.0448 = committed identically),
   replicates (rep1 +15.86/+14.11, rep2 +15.54/+12.24; 3-stream ν2.5 mean
   +15.290%, sd 0.723% — arithmetic re-derived). ALL MATCH.
2. **Gate mechanics re-derived:** clear set {1} on the adjudicating trained
   leg; counterfactual checked — on the edge leg oct2 (z=2.81) falls inside
   the 0.5·hypot at-bar band, so the verdict is NOT-CONFIRMED on EITHER leg.
   The orders' wording gap (no leg named — my drafting) was repaired by the
   executor PRE-DATA in the prereg (trained = adjudicating, edge =
   continuity); repair credited, gap noted against the orders' author.
3. **Process:** prereg → tests-first (one validation synthetic corrected
   PRE-DATA with disclosure — the Haar-response/aliasing lesson, a genuine
   instrument-ledger entry) → probes → pre-stated diagnostic ledger → D1–D4
   → drafted-not-run preregs → report. Bands fixed in-test before data;
   diagnostic budget 4/6 runs, 0 extra GPU; night GPU 0.03 H100-h. Test diff
   additions-only (2 new test files). Suites re-run at harvest: tests/ 39
   pass; tests_wfm+tests_p2 pass (exit 0). The temptation-ledger entry
   (refusing the edge-leg corroboration reading) is verified against the
   numbers and credited.

## Ruling

**GATE-N1 NOT-CONFIRMED is FINAL.** The multi-octave whiteness premise —
the N2 spec's basis — is measured absent; the refusal to train was correct
and the orders worked as designed.

**The N1 mechanism profile is ACCEPTED as committed evidence.** Adopted
reading: **M-GRAIN leading** — the generated octave-1 details on the real
field carry a broadband ~15% power deficit below Nyquist closing at the
pixel ring; composited, this yields pixel-scale barely-threshold maxima:
an excess that 0.5-px smoothing erases (+14.5% → −1.7%), that declines with
height, and that is present exactly and only where the whiteness is
(gowerstreet yes, sandbox no). M-COUPLING retired (gen ≥ real everywhere).
M-COMPOSITION open (transplant sign inversion unexplained — L3-family
material). Caveat carried verbatim: the causal evidence is a two-field
association plus mechanism consistency, not yet a dose-response curve.
Bonus finding logged: the nn spacing residual co-locates at the fine
octaves (hybrid 4.62/5.16 vs 2.00/0.92) — first structural link between
the count excess and the named frontier; if L1 moves BOTH, that is strong
mechanism evidence.

## Scorecard

- GATE-N1: executor 55, reconvene 60 — **both LOSE; my line was the worse
  one.** Accepted (executor Q3): both lines priced "premise present"
  (TRUE, 5σ) when the gate adjudicated "premise at ≥2 octaves" (FALSE).
  **Bar-ledger #13:** a gate line must be priced against the gate's literal
  rule, and when the rule is a sufficiency proxy for a premise, register
  BOTH lines separately (premise-present AND rule-fires).
- Morphology lines: smoothing-kill 60 HIT; height-growth 45 correctly on
  the minority side; oct1-largest-single-octave 55 INVERTED (unlisted
  direction again — transplant DEFICITS); co-location argmax 50 no-fire;
  replicates 80 HOLD.

## Orders (the four questions answered)

1. **L1 ADOPTED as the Stage-2 instantiation** — one-octave colored-t base,
   copula composition, D1-measured filter frozen pre-training, tests t1–t4,
   sandbox canary + kill criterion, ≥2 gowerstreet seeds, F2 sampling
   unchanged — with two amendments:
   - **A2 (the transfer rider, binding):** score the new variant's oct-1
     detail coloring C BEFORE the peak readout is opened (pre-statement
     order). Mechanical: TRANSFER iff C_gen(L1) − 0.7237 ≥ half the measured
     gap (i.e. C_gen(L1) ≥ 0.7551) at ≥3σ on its own SEs. This splits
     L1-NULL into NULL-WITH-TRANSFER (M-GRAIN wounded — the graininess was
     not the peak producer) vs NULL-WITHOUT-TRANSFER (the flow does not
     carry base spectra into details — the L2 loss-side family becomes the
     live lever). Bar-ledger #13 applied forward.
   - **A3 (reference):** the peak comparison target is the 3-stream
     reference (ν2.5 mean +15.290%, stream sd 0.723%; ν3.0 mean computed
     the same way from committed streams at pre-statement time, R12);
     per-stream bootstrap SE ~3.1% remains the CI basis; Δ convention as in
     N3 with the shared-real conservatism note carried.
   - Branch weights registered (reconvene / executor-draft): L1-CURED 10/10,
     L1-IMPROVED 35/40, L1-NULL 35/30 (A2 splits it), L1-REGRESSED 10/10,
     gates+FLIPPED 10/10.
   - **Pre-delegated review (Andreas's autonomy directive):** if the
     committed L1 prereg instantiates the draft + A2/A3 with no other
     deltas, it may run WITHOUT a further reconvene STOP; any delta →
     STOP as usual. Readout STOPs for reconvene adjudication regardless.
2. **L3 AUTHORIZED in parallel** (CPU, descriptive, all committed stacks):
   the coloring-vs-count-bias taxonomy curve. It does not touch the
   one-lever rule. If C predicts bias sign across the taxonomy, it enters
   the paper as methodology.
3. **L2 PARKED** pending L1's A2 readout — the doctrine debate is live only
   if transfer fails.
4. **Paper:** skeleton stays frozen (PLAN), but the graininess
   decomposition, the coloring index + its instrument lesson, and the
   cross-field association are DESIGNATED mechanism-section material now —
   committed, replicated, cross-field-tested; the reframe will absorb them
   regardless of L1's outcome.
5. **Stage-3 judge (executor's note ADOPTED, binding):** the Minkowski-
   functional scorer is built tests-first, validated on synthetics only,
   committed and FROZEN **before** the L1 training job launches; it is
   applied to NO generation (committed or new) until Stage 3. This keeps
   one tier of the paper's validation untouched by mechanism knowledge.

Sequencing: judge-freeze → L1 prereg (+A2/A3) → L1 canary/training/readout;
L3 anytime. Budget: 0.43/10 H100-h spent; L1 estimated ≤2.5; cap for this
order set: 4.
