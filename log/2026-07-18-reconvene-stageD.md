# 2026-07-18 — RECONVENE RULING: STAGE D ADJUDICATED — D-PASS-BOTH STANDS. The original bet is won, scale-blind. THE PAPER IS GO.

Audit: branch verified from stageD_verdict.json; replication all_pass verified;
figure inspected (cage demonstrably deployment-blind — selection saw octave 3
only); the mid-run env-split fix (pywt/JAX process separation) reviewed — infra,
not science; prereg discipline intact.

## R21 — Stage D: D-PASS-BOTH stands, on the honest reading

The formal kurtosis bars (3·SE_rel on a 32-field real reference) inflated to
56–59% and are near-vacuous; the result is adjudicated on the BARE floors
(10%/15%), where all 8 checks still pass — e2e unambiguous (kurt 3.2%/4.4%,
var_slope 4.7%/7.9%), arm A's head-conditional kurtosis inside by 0.1% (stated
whenever the result is). Bar-design ledger #9: reference-side SEs must be
budgeted when bars are set (32 real edge fields was too few for a 3·SE bar).
**P-D — the project's original pre-registered bet — PASSES under the deployment
protocol** (coupling-curve extrapolation, no measured target-octave couplings,
selection caged off the edge).

## R22 — the dial finding: the founding hypothesis dies so the mechanism can live

The dial-beats-scale-blind metric: FALSE (B 4.4% vs A 3.2% e2e kurt deficit).
Single-octave extrapolation works SCALE-BLIND — the coarse conditioning itself
carries the scale information the explicit coordinate was hypothesized to
supply. This is B1's r*≈1 locality confirmed at the deployment level, on the
question the project was born asking. Adopted framing for the paper: the drift
is real and measured (stage-0 stands untouched); the *architecture* does not
need to be told about it at one octave, because the information is local to the
conditioning. The dial is NOT inert: it changes JOINT structure — the peak-audit
excess flips sign under B (over-correction −9/−17%) vs A's up-tilt (+13/+15%) —
neither arm closes the third audit tier. Two-octave extrapolation degrades
(−44/−54%): each octave of extrapolation costs; the validated domain remains ONE
octave, now with the cost curve measured.

## R23 — replication stands; the calibration claim is properly supported

24/24 on 64 fresh fields with ~√2 tighter bars. Named residual adopted: arm B's
octave-4 tails run OVER (41% error riding a wide bar) — arm A is clean
everywhere (≤12.4%). Consequence: **arm A (scale-blind) is the reference
generator for the paper's demonstrations**; B appears where the dial's
joint-structure effect is discussed.

## R24 — calibration ruling: the failure-era priors are retired

Fourth consecutive under-confidence miss on a constructive outcome (executor
modal D-FAIL-BOTH 55 vs D-PASS-BOTH fired at 18; the PLAN's ORIGINAL P-D 40 was
the best-calibrated number in the room). For the writing phase and any phase-3:
base rates for post-diagnosis interventions on this substrate are re-anchored to
the measured 4-for-4 record; the scorecard section of the paper reports the
under-confidence pattern as a finding about pre-registered science with agents.

## R25 — THE PAPER IS GO (per PLAN §4 branch-complete and R20.3)

One paper: measurement (drift 2-D + locality r*≈1 + N_eff≈parents) →
moment-ladder mechanism (causal at rungs 2 and 4; base-erasure) → the three-tier
audit (power → marginals → joint placement, each tier invisible to the one
below) → the audit-guided rescue arc (data cure; head retirement via forensic;
t-base + caged selection) → Stage D: single-octave scale extrapolation works,
scale-blind, on the audited substrate; joint structure and multi-octave are the
measured frontiers. Binding constraints carried forward: Gate-0 claim wordings
incl. the 07-17 scope revisions; Schanz-systematizing positioning; scope words
(one octave, 2D WL, one seed-family, arm A reference); R12 numbers-by-copy; the
A-hc 0.1%-margin statement; bar-ledger disclosures. Writing plan:
SPEC-paper-skeleton.md (committed alongside this ruling). Estimator-hardening
leftovers (starlet-ℓ1, nanmedian, SC bands) fold into the writing phase as
figure-hardening tasks — they no longer gate anything else.

Eleven days from "it doesn't really work, does it?" to the original bet passing
on a substrate that survives its own audit. The record is the argument.
