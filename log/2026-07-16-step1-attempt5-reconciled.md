# 2026-07-16 — Step 1 reconciliation: attempt 5 ALREADY READ OUT (race condition in the rulings)

## The race condition, surfaced

Both 2026-07-16 reconvene documents (`reconvene-bigpicture-audit.md`,
`reconvene-reconcile-audits.md`) treat attempt 5 as pending: "proceed on attempt 5",
"P-attempt5-lever 55% → 40% ... re-registered here at the new value before the run",
"until attempt 5 reads out". **Attempt 5 ran and read out on 2026-07-11** — jobs
15762584/15762585, hashes ffdeac4d4b/ccc86ccf1b, prereg
`log/2026-07-11-prereg-attempt5-selfcond.md` (bars exactly as ruled in the 4bpii
ruling; weighted gate branches committed BEFORE the run), readout committed at
`e96ce89`, five days before the rulings. The rulings' evidence inventories cite only
4b′/4b′-ii measurements, confirming the readout was not in their context.

## Consequences (discipline applied, not judgment)

1. **The 55→40 re-registration is VOID** — a prediction cannot be re-registered after
   the outcome exists. The binding pre-registered predictions remain those committed
   pre-run on 2026-07-11: reconvene 55%, executor 35% (B-distribution
   B1 20 / B2 15 / B3 25 / B4 35 / B5 5). Outcome: **B5** (lever failed with
   degradation).
2. **Step 1 of the reconciled program is COMPLETE.** The ruling's own discrimination
   criterion — "if [self-conditioning] fails, the informational-limit reading gains
   the status of a tested negative" — is met by a pre-registered experiment: the
   discriminator measured end-to-end = drifted-input response for the 4a model
   (0.742 vs 0.746), the honest conditional given drifted coarse at 0.53, and
   degradation when honesty is trained in. No rerun; nothing to re-adjudicate.
3. **Language discipline adopted, including against our own record.** The
   reconciliation ruling is right that "proven informational" (used in the attempt-5
   commit message and RESULTS heading) over-reached even with the readout: attempt 5
   is a tested negative for TRAIN-SIDE conditioning fixes in this data regime, for
   this architecture — not a proof about all generators. Binding wording from now:
   **"a measured compounding cap consistent with an informational limit; train-side
   rescue excluded by a tested negative (attempt 5); scope to be bounded by the
   self-similar control."** RESULTS-phase1c.md §attempt-5 wording corrected
   accordingly. The reconvene's substantive correction stands; only its procedural
   framing (re-running) is moot.

## Program state after reconciliation

Gate 0 (kill-tests, SPEC-novelty-collapse.md — now covering both claims) → parallel
cheap session, gates all paper claims. Step 1 → DONE (above). Next here, one at a
time: Step 2 inverted validation pilot on the frozen generator; Step 3
downstream-bias demo (prereg target statistic + criterion first); Step 4 self-similar
control (taken up: cheap, and it bounds the tested negative's scope); then the
reshape memo (with the executor's chat audit committed as its appendix). STOP after
the memo.
