# 2026-07-31 — DECISION: scope fork resolved — OPTION A (Andreas)

Andreas has decided, concurring with both recorded recommendations
(SCOPE-FORK-MEMO.md): **Option A — the declared-resolution close.**

Binding consequences:
1. **Final configuration:** committed arm-A checkpoints + F2 group-averaged
   sampler + the deployment-pure deconvolved oct-1 base (the validated
   calibration tool). This is the config the paper ships.
2. **Peak claim:** calibrated at declared resolution σ_s ≥ 0.5 px (FWHM
   ≈ 1.2 px) at trained scales and the extrapolated edge; native-resolution
   counts declared outside the validated domain with the located mechanism
   (pixel-scale phase texture, spectrum-independent at fixed weights).
3. **Spacing row:** restated on pooled multi-stream measurements (the
   stream-max deflation, decision-package probe 1).
4. **Option B** (training-side attack on the native-resolution texture) is
   parked as post-paper follow-up; its first step, if ever taken, is a
   committed phase-domain instrument.
5. **Phase 3 proceeds to Stage 3** (PLAN-phase3, as amended by the arc):
   (a) training-seed ensemble n≥3 of the full final recipe — the
   robustness table; (b) the ONE-SHOT pre-registered blind re-run of the
   full edge protocol on the final configuration with smoothed-peak
   entries — the paper's headline table; (c) the frozen Minkowski judge
   applied for the first time — the untouched tier; (d) the isotropy/
   stationarity declared-domain caveat into the method section. The blind
   run's prereg REQUIRES reconvene review before submission (one-shot;
   no pre-delegation). Budget: ~2 of the remaining ~9.4 H100-h.
