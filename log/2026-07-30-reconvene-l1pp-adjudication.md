# 2026-07-30 — RECONVENE R41: L1″ adjudicated — N-NULL-PT-CONFIRMED stands;
# the transfer model is quantitatively VALIDATED; the peak excess is
# spectrum-independent at fixed weights; spectral levers EXHAUSTED for the
# peak tier; decision package ordered before the scope fork goes to Andreas

Ruled on: the READOUT appended to log/2026-07-30-prereg-l1doubleprime.md
(91bf2b4) + l1pp_* artifacts. Same-day git log taken; queue empty.

## Verification

1. **R12:** P-T artifact re-checked verbatim — C_pooled 0.7414±0.0048 in
   the pre-committed [0.7192, 0.7919] (prediction 0.7556); directional
   +0.0177 = +3.6σ; oracle 0.7815±0.0103 vs its committed prediction
   0.7819 (0.04σ; real 0.7864 at ~0.5σ). Peaks pooled +13.82%±2.92 /
   +13.66%±2.68 (Δ 0.34 / −0.31); oracle stream +15.75/+12.91 at
   restored-real coloring. Identity gates, canary bars (watched oct2
   kurtosis 13.8% IMPROVED from 17.3%), starlet leg 10 — ALL MATCH.
   P-T-before-peaks enforced structurally (l1pp_pt.json written first).
2. **Branch mechanics re-derived:** P-T lands → the NULL split keys
   confirmed; both Δ < 2, no at-bar, no flip, no regression →
   **N-NULL-PT-CONFIRMED is the unique mechanical outcome.** Suites re-run
   at adjudication: tests/ 44 pass; tests_wfm + tests_p2 pass (exit 0).
3. **Prereg discipline:** R40 instantiated exactly (pre-clearance
   correctly consumed); the honest power note (deconvolved base nearly
   white, |pred − baseline| = 0.88× band) plus the separately registered
   directional line was exemplary bar-craft — the directional line is what
   made the adjudicating-stream result INFORMATIVE rather than merely
   band-compatible. Credited.

## Ruling — adopted verdict-grade findings

1. **The multiplicative transfer model is VALIDATED as quantitative,
   predictive theory:** third input point inside a pre-committed band;
   the oracle deconvolution lands on its prediction at 0.04σ and restores
   the real coloring within ~0.5σ. The system identification of R40 is no
   longer a model — it is a measured property of weight-tied extrapolation
   on this field.
2. **The oct-1 peak excess is INDEPENDENT of the oct-1 detail power
   spectrum at fixed weights.** Three coloring doses (C 0.603 → 0.741 →
   0.782-oracle), nine streams, same ~+14–15% excess throughout.
   **M-GRAIN's spectral form is REFUTED as the peak mechanism**; combined
   with N1's smoothing kill, the surplus is pixel-scale structure living in
   the PHASE/higher-order content of the generated fine details. Third
   clean dissociation of the campaign: parity ✂ peaks, coloring ✂ peaks.
3. **Inference-only spectral levers are EXHAUSTED for the peak tier** —
   adopted as a scope statement of the record.
4. **The deconvolution ships as a validated deployment calibration tool**
   for the coloring defect (no retraining; deployment-pure ingredients;
   canary kurtosis also improved). Whether the FINAL configuration ships
   with it is decided by the package below, not assumed.

## Scorecard

- Peak modal: **reconvene IMPROVED 38 MISS; executor NULL 45 HIT** — and
  the hit was earned, not lucky: their registered reasoning extrapolated
  the L1′ measured dose-response (peaks flat across C 0.585–0.630) where I
  extrapolated the mechanism hope. **Calibration note (standing): a
  measured dose-response outweighs a mechanism-based expectation, even a
  well-motivated one.** P-T lines 70/65 both HIT; directional 60 HIT.
- nn_T sequence 4.045 → 3.33 → 2.90 across colorings remains UNINTERPRETED
  (stream-scatter unknown) — promoted to the decision package.

## Orders — the decision package (cheap; runs BEFORE the fork memo)

The phase now faces a scope fork that belongs to Andreas: (A) close the
peak tier by DECLARED RESOLUTION (peaks calibrated at the smoothing scales
the field actually uses — N1 measured −1.7±2.3% at 0.5 px on white-base
maps) + proceed to Stage 3 hardening; or (B) training-side redesign (the
L2 family / training through oct-1 — expensive, reaches oct-1 only via
weight-tying, touches the founding bet). The fork memo goes to him WITH
measurements, not speculation:

1. **nn stream-scatter from disk (CPU, zero GPU):** nn_T per stream on ALL
   existing trained-leg streams (3 white + 3 L1′ + 3 L1″ + oracle) — the
   instrument's stream variance AND the dose trend, from committed maps.
   Pre-register the read: does the white-vs-deconvolved difference exceed
   the measured stream scatter? If YES, the spacing frontier responds to
   coloring even though counts do not — a mechanism finding AND a reason
   the final config ships the deconvolved base.
2. **The resolution-scoped peak audit (CPU on committed maps):**
   smoothing-scale sweep σ_s ∈ {0.25, 0.5, 0.75, 1.0} px on the CANDIDATE
   final config (the L1″ deconvolved streams + oracle) AND the committed
   white-base streams, trained leg + the stage-D edge leg, both ν, CIs,
   per-parent panel — locate the zero crossing and establish whether a
   declared-resolution pass exists for BOTH configs at BOTH legs.
3. Both pre-registered with expectations+weights (mine at review if the
   executor STOPs, or alongside if it instantiates directly — this order
   pre-clears the package as specified; any delta → STOP). Artifacts →
   results_p2/l1pp_decision_*. Zero training; zero-to-negligible GPU.
4. THEN the fork memo: one page, both options with measured costs and
   what each claims, executor recommendation + reconvene recommendation
   recorded separately, STOP for Andreas.

Budget: order-set spend 0.11 H100-h; the package adds ≈0. Paper skeleton
remains frozen; findings 1–4 designated mechanism-section material.
