# 2026-07-25 — RECONVENE RULING R35: disambiguation clean; arm D adjudicated with a seed-confound downgrade; one confirmation pass and the experimental program CLOSES

1. **F2 disambiguation: the at-the-bar component was a fluctuation** (z=−0.01 at
   64 fields; all F2 statistics null-consistent). Recorded per the pre-stated
   rule, no retro-upgrade — the branch history keeps its honesty and the
   symmetrized sampler is, in substance, clean.
2. **Arm D: the mechanical branch (D-SHRINK, at the bar by 0.0135) fired, but
   its MEANING is downgraded — the seed rider did its job.** N1-seed1 (6.86)
   landed BELOW the 8× arm (7.64): the committed 1× baseline (15.3) carries
   ~2× seed variance, so every shrink ratio computed against it is
   seed-confounded. Adopted reading: for THIS estimand at these scales, seed
   noise is comparable to the data effect, and no clean data-causality claim
   issues from the ratios. What survives seed noise (within-arm, curve-level):
   **data lowers the late plateau and opens a joint-viable window at 32×**
   (T=2.88@17500 — the first sub-bar joint reading of the campaign, val-side).
3. **The mechanism finding is the keeper: the cage picks spikes at EVERY data
   scale** (n32's selection landed on a 1500-step val spike of 20.2 while its
   own late curve sits at 2.9). Combined with (2): **data widens the healthy
   windows; selection still misses them.** The designated cure is the joint
   selection criterion operating at high data — J's criterion finally has a
   regime where a joint pick can exist. This completes the schedule story's
   third act.
4. **Currency: CUR-PARENTS confirmed (1.417 at matched picks)** — independent
   initial conditions beat conditional multiplicity as the data currency. The
   PM-program design directive is now measured, not argued. Both parties'
   modals HIT (first clean double since the starlet sweep).
5. **Ledger entries.** #11: three consecutive at-the-bar verdicts — bars set as
   fixed multiples of measured references keep landing inside estimator noise;
   future bars carry a pre-declared ambiguity band with
   disambiguation-by-replication (the de-facto procedure, now codified).
   Seed lesson: single-seed causal arms on high-variance estimands are
   underpowered — within-arm (paired, curve-level) comparisons are the robust
   design; stated in the paper's limitations.
6. **Scorecard:** D-WIDEN modals missed both sides (45/40; D-SHRINK 25/25
   fired — ninth theme instance); CUR-PARENTS hit both sides.
7. **Orders (the last experimental ones):** (a) MANDATORY: test-side
   confirmation of the 32× joint window — score the test verdict at n32's
   late window with the frozen joint criterion (zero training); (b) OPTIONAL,
   executor's discretion within 1 GPU-hour: a 3-seed N1 mini-ensemble of the
   estimand to price seed variance properly for the record. On (a)'s readout,
   **the experimental program of this phase CLOSES** — all remaining work is
   WP1 writing (which now absorbs: the schedule trilogy, the F/F2 cure arc,
   the currency directive, the nn frontier, and the ledger) pending Andreas's
   structural read.
