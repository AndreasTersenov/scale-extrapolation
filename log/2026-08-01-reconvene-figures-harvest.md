# 2026-08-01 — RECONVENE R45: figure-pass harvest — galleries + story
# figures accepted; the topology decomposition is a NEW mechanism finding
# and forces a §5 wording amendment; its numbers need an artifact (R12)

Harvested: 1613ff8 (story figures + F20 delivery), c9a625d (clean
galleries), 6a220bc (topology explainer). All within the authorized
figure-work scope; scripts committed, inputs = committed maps only.

## The finding (descriptive, currently artifact-less)

The E3 failure decomposes: T_MF = 6.35 is carried by V2 (Euler
characteristic) at ν ≈ 0 — MID-LEVEL topology — with a +4.5σ V1
boundary-length component and a sign flip at ν = −1. On the maps: the
generated fields fragment into MORE connected components (~450 vs ~390 at
the χ peak; per-map χ(0) +240.3 vs real +193.4) and FEWER holes (167 vs
193, tile-3 exhibit). This is NOT the high-ν pixel-scale tail where the
peak-count mechanism lives.

## Orders

1. **A7 (R12):** emit the decomposition as a committed artifact
   (results_p2/phase3_topology_decomp.json: per-ν V0/V1/V2 z-curves, χ
   curves with SE bands, component/hole counts, per-map χ(0) populations)
   from the committed script; verify the quoted numbers match.
2. **A8 (paper §5 wording amendment, binding):** the current boundary
   text calls the MF failure "continuous with the located pixel-scale
   phase-texture mechanism." The decomposition does not support asserted
   continuity: mid-level connectivity (excess islands, hole deficit at
   ν≈0) is a DISTINCT located signature from the ν ≥ 2.5 pixel-scale
   count excess. Amend to: two located boundary signatures — (i)
   pixel-scale phase texture (native peaks, mechanism measured), (ii)
   mid-level topology (χ/V2 at ν≈0, first-applied judge) — with their
   relationship stated as OPEN. This is the more honest AND stronger
   text: the untouched judge found a defect the peak tier could not have
   seen even in principle at its thresholds.
3. Galleries + story figures go to Andreas with this harvest (raw-eyes
   rule) as part of his structural-read packet.

No branch adjudication (all descriptive); no GPU spent; suites untouched
(no estimator changes — plot scripts only).
