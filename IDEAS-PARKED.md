# PARKED IDEAS — write here instead of running them

Rule from HANDOFF.md §4: no new experiments beyond the task queue. When a
session or a thought proposes one, it lands here with a date and a
one-paragraph rationale, and stays until Andreas revives it deliberately.

Already parked, with their rulings:
- **PM/N-body super-resolution** (the real ambition). Gated on paired
  matched-IC data (FastPM/COLA + N-body). The regime is extrapolation-FREE,
  so the campaign's hardest defect largely does not apply — but the
  architectural phase-coherence floor does, in milder form. Next project.
  See BRIEF-foundations.md #1, PROJECT-EXPLAINER.md §8.
- **Mass-mapping prior (PnP).** The cascade as a prior for shear→convergence
  inversion; attractive because the data term anchors the modes our texture
  defect affects. Closest "someone is stuck" application in Andreas's own
  lineage. Unscoped.
- **Texture-transfer corrector** (R47 order 4, still draft-only): measure
  the phase-texture statistic octave-by-octave in the trained range, model
  its transfer, extrapolate one octave, correct the output. The spectral
  calibration playbook on the phase axis. Needs the instrument (built) plus
  its own prereg.
- **Cascade training** (D4, lowered prior after the inference probe null).
- **TIDAL at depth 2** (D3; machinery built, proven, shelved).
- **Global-context / VAR-style receptive field** — only the free diagnostic
  (effective receptive field vs defect correlation length) is worth doing.
- **WPH phase-structured base** — Stage 0 (scale-regularity of WPH
  covariances) is a cheap kill-test if the phase question reopens.
- **Autoguidance at the finest octave**, **conditioning-noise augmentation**,
  **physical-constraint tiers 2–3**: cheap, ranked 6–8 in BRIEF-foundations.
